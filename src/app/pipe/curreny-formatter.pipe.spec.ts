import { CurrenyFormatterPipe } from './curreny-formatter.pipe';

describe('CurrenyFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenyFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
